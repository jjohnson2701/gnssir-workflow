# ABOUTME: Minimal requirements file for pip installation.
# ABOUTME: For full dependency management, use: pip install -e .[all]
#
# Core dependencies - these are required for basic functionality.
# See pyproject.toml for version constraints and optional extras.
#
# Install options:
#   pip install -e .              # Core only
#   pip install -e .[dashboard]   # Core + Streamlit dashboard
#   pip install -e .[animation]   # Core + animation generation
#   pip install -e .[dev]         # Core + development tools
#   pip install -e .[all]         # Everything

# Core dependencies
gnssrefl>=3.12.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
dataretrieval>=1.0.0
requests>=2.28.0
geopy>=2.4.0
tqdm>=4.65.0
